image: node

stages:
  - lint

eslint:
  tags:
    - docker
  stage: lint
  script:
    - cd ./source/server/
    - npm ci
    - npm run lint
    - cd ../../
    - cd ./source/client/
    - npm ci
    - npm run lint

    build-shop:
  stage: build_shop
  script:
    - echo "Hello onlineshop steps"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      changes:
        - onlineshop/**/*

